"""
  selector
    The smallest selector used to target the content.

  slot_name
    The mixin used in less (the "slot name").

  slot_context
    The context parameter used in less mixin parameterization (the "slot context").

  has_parent
    If this is False the content type only contains other content. The content
    will not be nested into another type of content. An example is a Preface.

  has_children
    If this is False the content type does not contain other elements, but it may
    be nested itself. An example is an Image.
"""
